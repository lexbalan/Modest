

import "libc.h"


func mtab (n : Nat32) -> Void {
	var m : Nat32
	m := 1
	while m < 10 {
		let nm = n * m
		printf("%d * %d = %d\n", n, m, nm)
		m := m + 1
	}
}


func main () -> Int64 {
	printf("Hello World!\n")
	mtab(2)
	return 0
}

