

include "libc.h"


func mtab (n : Nat32) -> Void {
	var m : Nat32
	m := 1
	while m < 10 {
		let nm = n * m
		printf("%d * %d = %d\n", n, m, nm)
		m := m + 1
	}
}


func main () -> Int64 {
	let n = 2
	printf("multiply table for %d\n", n)
	mtab(n)
	return 0
}

