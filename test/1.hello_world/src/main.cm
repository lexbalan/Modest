// examples/1.hello_world/main.cm

import "libc/stdio"

const hello = "Hello"
const world = "world!\n"

const hello_world = hello + world


func main() -> Int {
    printf("%s", hello_world to *Str8)
    return 0
}

