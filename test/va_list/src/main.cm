// test/va_list/src/main.cm

import "libc/stdio"
import "libc/unistd"

/*@attribute("c-no-print")
import "lightfood/print"
@c_include("./print.h")*/


@attribute("value.type.to:dispensable")
func my_printf(format: *Str8, ...) -> SSizeT {
	var va_list: VA_List

	__va_start(va_list, format)

	let a = __va_arg(va_list, Int32)

	__va_end(va_list)

	/*
	var va_list_ptr: VA_List
	va_list_ptr = list_ptr

	let maxstr = 128+1
	var buf: [maxstr]Char8
	//let n = vsnprintf(&buf, maxstr, format, va_list)
	let n = __vsnprintf_chk(&buf, maxstr, 0, maxstr, format, va_list)
	return write(c_STDOUT_FILENO, &buf, SizeT n)*/
}


func main() -> Int {
	//print("Hello World!\n")

	var k = 10
	my_printf("My Printf Test %d\n", k)

	/*let c = Char8 "$"
	let s = *Str8 "Hi!"
	let i = Int32 -1
	let n = Nat32 123
	let x = Nat32 0x1234567F

	print("\{\}\n")
	print("c = '{c}'\n", c)
	print("s = \"{s}\"\n", s)
	print("i = {i}\n", i)
	print("n = {n}\n", n)
	print("x = 0x{x}\n", x)*/

	return 0
}

